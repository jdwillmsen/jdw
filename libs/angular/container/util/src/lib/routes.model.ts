export type RouteRemote = {
  path: string;
  name: string;
  id: string;
  entry: string;
};

export type ComponentRemote = {
  name: string;
  id: string;
  entry: string;
};

export type NavigationItem = {
  path: string;
  icon: string;
  title: string;
  description: string;
};
